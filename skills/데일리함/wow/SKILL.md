---
name: .⚡wow
license: Apache-2.0
metadata:
  version: 2.1.0
description: 더 단순한 설계 제안
---

<!-- common-rules:start -->

## 공통 판단 기준

현재 요청과 이 스킬의 작업 범위 안에서 관련 있는 기준만 적용한다. 규칙을 충족하려고 요청 밖 수정이나 형식적인 보고 항목을 추가하지 않는다.

설계 대안을 비교하는 기준으로 적용한다. 상태·분기가 줄어도 변경 영향이 넓어지는 안은 비용을 함께 설명한다.

### 한 번에 다루는 맥락 줄이기

함께 실행되지 않는 분기와 서로 다른 목적의 로직이 교차하면 책임별로 분리한다. 구현 상세를 숨길 때는 호출부에서 목적을 이해할 수 있는 이름과 경계를 선택한다. 단순히 함수가 길다는 이유만으로 나누지 않는다.

### 시점 이동 줄이기

동작을 이해하기 위해 여러 함수·파일을 왕복해야 하는지 본다. 단순한 정책은 사용처에서 위에서 아래로 읽히게 표현하고, 추상화가 필요한 경우에도 핵심 흐름은 호출부에 드러낸다.

### 조건과 숫자의 의미 드러내기

복잡한 조건과 맥락 없는 숫자에는 의도를 설명하는 이름을 붙인다. 중첩된 삼항식은 조건의 순서가 보이도록 펼친다. 이미 자명한 한 줄 표현을 이름 붙이기만 위해 분리하지 않는다.

### 숨은 부수효과 확인하기

조회·검증처럼 보이는 호출에 로깅·상태 변경·화면 종료가 숨어 있는지 확인한다. 수정하는 작업에서는 호출부나 명시적인 책임으로 드러내되, 실행 순서·실패 전파·관측 동작을 보존한다. 읽기 전용 작업에서는 실제 영향과 의도된 동작인지 판단하는 근거로 사용한다.

### 이름과 동작 일치시키기

이름·입력·출력으로 실제 동작을 예상할 수 있는지 본다. 기존 라이브러리와 동작이 다른 래퍼는 추가 책임을 드러내는 이름을 사용한다. 이름을 맞추려고 공개 계약을 임의로 바꾸지 않는다.

### 같은 역할의 반환 형태 맞추기

같은 역할의 함수·Hook은 반환 형태를 일관되게 유지한다. 데이터와 조회 상태 객체, boolean과 결과 객체를 혼용해 호출자가 내부 구현을 확인해야 하는지 본다. 의도된 계약 차이는 보존하고, 변경할 때는 기존 호출자의 의미를 확인한다.

### 변경 단위에 맞춰 경계 정하기

함께 수정·삭제되는 코드는 가까이 두고, 기능보다 파일 종류만으로 흩어 놓지 않는다. 폼도 필드가 독립적으로 변하는지, 필드 간 정책이 함께 변하는지에 따라 검증 경계를 정한다. 기존 import 방향·공개 계약·번들·라우팅·서버 경계와 스타일 우선순위를 유지한다.

### 함께 변경되는 단위 파악하기

같은 정책을 바꿀 때 반드시 함께 수정되어야 하는 부분을 찾는다. 코드와 사용처·설정·검증·공유 상수의 관계를 확인하고, 한쪽만 변경되면 깨지는 단위를 함께 다룬다. 모양이 비슷하다는 이유만으로 같은 변경 단위라고 판단하지 않는다.

### 변경 영향과 전달 계층 줄이기

공유 함수나 상태를 바꿀 때 영향받는 호출자와 화면을 확인한다. 같은 기술을 사용한다는 이유로 서로 다른 책임을 한 함수에 모으지 않는다. 값을 전달만 하는 중간 계층은 조합으로 줄일 수 있는지 보고, 실제 역할을 표현하는 props는 유지한다. 공유 상태·Context 도입은 필요한 소비 범위와 의존성이 명확할 때 선택한다.

### 독립적인 변경 보존하기

공통화는 같은 이유로 변하는 코드에 적용한다. 제품 의미가 다르거나 독립적으로 바뀌는 코드는 중복을 허용한다. 작은 중복을 없애려고 더 큰 추상화·순환 참조·역방향 의존성을 만들지 않는다. 함께 바뀌지 않으면 오류가 나는 경우에는 응집도를, 독립적인 경우에는 읽기 쉬움과 좁은 변경 범위를 우선한다.

출처: [Toss Frontend Fundamentals](https://frontend-fundamentals.com/code-quality/code/)의 코드 품질 기준을 작업별로 재구성했다.

<!-- common-rules:end -->

# .⚡wow

현재 구조를 전제로 삼지 않고 문제를 더 단순하게 정의한다. 사라지는 상태·분기·책임이 분명한 설계를 찾는다.

## 요청과 범위

- `$.⚡wow`, `/.⚡wow`의 기본 결과는 제안이다. 대상은 사용자 지정 파일·기능을 우선하고, 없으면 현재 diff를 본다.
- 설계 제안만 요청했다면 파일을 수정하지 않는다. "보고 마음에 들면 구현"이면 선택을 기다린다.
- "좋은 안을 골라 구현까지"처럼 선택과 구현을 맡겼다면 추천 이유를 설명하고 같은 요청 안에서 일반 구현 작업으로 이어간다. 이미 맡긴 작업을 다시 승인받지 않는다.
- 기존 구조 안의 코드 정리는 `.⚡good`, 화면의 시각·UX 개선은 `impeccable`의 범위다. 단순 불만이나 화면 디자인 요청만으로 재설계를 시작하지 않는다.

## 설계 판단

사용자가 얻어야 할 결과와 유지할 동작을 먼저 이해한다. 그다음 조건이 몰린 곳, 상태 동기화, 여러 계층이 같은 결정을 반복하는 곳에서 불필요한 개념을 찾는다.

- 여러 boolean을 하나의 도메인 상태로 표현할 수 있는가?
- 같은 결정을 여러 곳에서 하는 대신 한 곳이 책임질 수 있는가?
- helper를 더 만드는 대신 helper가 필요 없는 데이터 흐름으로 바꿀 수 있는가?
- 직접 만든 캐시·큐·라우팅을 현재 환경의 검증된 기능으로 대체할 수 있는가?

제안은 두 기준으로 판단한다.

1. 사라지는 상태, 분기, 파일 또는 책임을 구체적으로 설명할 수 있다.
2. 현재의 까다로운 사례를 새 모델에서도 처리할 수 있고 확인할 작은 실험이 있다.

가장 강한 안을 추천한다. 강한 안이 하나면 하나만 제시하고, 약한 대안으로 개수를 채우지 않는다. 이름 변경·파일 이동·단순 helper 추출만으로 끝나는 안은 제외한다. 제안의 구체성은 [references/example.md](references/example.md)를 참고한다.

## 보고

현재 문제가 생기는 이유에서 새 모델로 자연스럽게 이어 설명한다. 삭제되는 복잡성, 유지되는 사용자 동작, 중요한 위험과 가장 작은 확인 방법을 포함한다. 고정된 보고서 항목을 반복할 필요는 없다.

대상이나 핵심 제품 제약이 없어 결과가 크게 달라질 때만 질문한다. 자료로 확인할 수 있는 내용은 먼저 조사한다.
